#include <iostream>
#include "helper.h"

int main()
{
    srand((unsigned)time(0)); //So random numbers will be always random.
    helper SNAKE; //creating class m
    SNAKE.Start(); //Starting main function/
};